import { CssBaseline } from '@mui/material';
import { Header } from '../../../widgets';

const Main = () => {
    return (
        <>
            <Header />
            <CssBaseline />
        </>
    );
};

export default Main;
